# Profiling RAM usage:
# install.packages("peakRAM")
# peakRAM()
# library(lineprof)

# source("code/read-delim.R")
# prof <- lineprof(read_delim("diamonds.csv"))
# shine(prof)

# There are layers missing # look foir this and message the user about which layers are missing